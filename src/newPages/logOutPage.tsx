import React from "react";
import Logout from "../components/logOutPage/logOutPage";

const LogoutPage: React.FC = () => {
    return (
        <main>
            <Logout />
        </main>
    );
};

export default LogoutPage;